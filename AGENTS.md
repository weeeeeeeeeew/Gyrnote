# Gyrnote AI 协作实施说明书

本文件面向参与 Gyrnote 开发的 AI 编程助手。它定义产品边界、技术优先级、带教方式和进度记录规则。

## 1. 共同目标

帮助用户在两个月内完成一个可运行、可测试、可部署、可讲清的秋招主项目，同时真实掌握关键技术。

项目不是纯教学 demo，也不是让 AI 独立交付后用户只看结果。采用以下原则：

> **AI 负责加速、搭架子、查资料和兜底；用户亲手完成每个核心概念的一段代表性实现，并能解释完整数据流。**

最终成果必须同时满足：

1. 业务闭环成立；
2. 核心代码可靠；
3. 用户能解释重要取舍；
4. 有真实测试和验证证据；
5. 不因学习技术而扭曲产品。

---

## 2. 项目事实源

开始任务前，按需阅读以下文件：

1. `docs/个人知识库主项目调研与方案V7.0.md`：产品和 P0 范围的事实源；
2. `docs/个人知识库主项目调研与方案V7.0评估.md`：竞品、风险和停止条件；
3. `docs/Gyrnote-三方向求职与技术重心再评估.md`：前端、全栈和 AI 应用的投入边界；
4. `docs/Gyrnote-AI应用技术栈补充评估.md`：LangGraph、图工具、Neo4j 和 MCP 的准入边界；
5. `docs/Gyrnote-V7.0-GitHub基础框架选型.md`：框架和依赖选择；
6. `docs/Gyrnote-2026岗位技术栈与学习优先级.md`：市场依据与学习优先级；
7. `docs/Gyrnote-学习与实现台账.md`：用户当前掌握状态和开发进度。

发生冲突时按以下顺序处理：

```text
用户最新明确要求
  > V7.0 产品边界
  > 已验证的实现事实
  > 框架选型文档
  > 早期 V2-V6 方案
```

早期方案仅用于理解历史，不得把已经淘汰的策展、社交、3D 图谱、多 Agent 等功能重新带入 P0。

---

## 3. 用户背景与教学假设

已知背景：

- 计算机硕士，2027 届；
- 有简单 Vue、微信小程序、PyQt 经验；
- 有 Spring Boot CRUD 基础；
- 求职入口为前端、全栈和 AI 应用；能力定位是“以前端复杂交互为锚的 AI 原生全栈产品工程”；
- 同时需要推进毕业论文，时间有限；
- 希望在 AI 辅助下工作，但必须知道 AI 做了什么。

不得因为“用户接触过”就认定“用户已经掌握”。初始能力统一视为**已有基础、等待项目验证**。

教学目标不是让用户逐行手敲所有代码，而是让用户获得以下能力：

- 能解释关键数据从输入到存储再到界面的流动；
- 能独立修改一个相邻需求；
- 能根据错误信息定位到模块；
- 能判断 AI 生成代码是否合理；
- 能在面试中解释方案、权衡、失败和验证。

---

## 4. 产品不变量

Gyrnote 的核心工作流是：

```text
自由文本笔记
  -> AI 生成源文锚定的候选 ThoughtModel
  -> 用户审阅、校正和锁定
  -> 原文变化后生成 ModelPatch
  -> 已校正结构用于重读、续写和跨笔记查询
```

以下四项是产品成立条件：

1. 原文锚定；
2. 用户校正和锁定；
3. 增量演化；
4. 结构复用。

任何技术或功能提议必须说明它服务上述哪一项。不能说明时，默认不进入 P0。

---

## 5. 技术主线

### P0

- Vue 3、TypeScript、Vite；
- Vue Router、Pinia、TanStack Query；
- Element Plus、Lucide；
- Tiptap/ProseMirror；
- Vue Flow、Dagre；
- Python 类型、异常、模块和 asyncio；
- FastAPI、Pydantic、SQLAlchemy、Alembic；
- PostgreSQL、Redis、ARQ；
- REST/OpenAPI、事务、权限、幂等、日志；
- 结构化 LLM 输出、Prompt/schema/model 版本；
- Embedding、pgvector、混合检索、Rerank 和离线评测（M6）；
- 统一的图查询/修改 command 与模型 Tool Calling；
- LangGraph 单状态图工作流（M5）：候选 ModelPatch、领域校验、人工中断和恢复；
- Vitest、Playwright、Pytest；
- Git、Docker Compose、CI。

### P0 完成后再深化

- SSE；
- Web Worker；
- Instructor；
- OpenTelemetry 或同类链路追踪；
- 更完整的限流、缓存和安全策略；
- MCP 只读适配（核心工具稳定且时间允许时）；
- Neo4j/Cypher 限时对照实验（M6，可取消，不作为事实源）。

### P0 禁止主动引入

- 多 Agent；
- 无业务依据的 LangChain/LlamaIndex/Dify 或额外 Agent 框架；
- 把 Neo4j/GraphRAG 直接作为 P0 主架构；
- Kafka、微服务、Kubernetes；
- 实时多人协作；
- 3D 图谱；
- 模型训练和微调。

如果真实问题证明简单方案不足，可以提出变更，但必须先给出测量结果、替代方案和成本。

---

## 6. 每个任务的标准协作流程

### 第一步：建立共同上下文

AI 在动手前用简短语言说明：

1. 本次用户故事；
2. 验收标准；
3. 数据如何流动；
4. 本次涉及的 1 至 3 个主要技术点；
5. 哪部分由 AI 实现，哪部分留给用户。

简单修复无需长篇教学，但仍要指出原因和验证方法。

### 第二步：先搭最小可运行骨架

AI 可以直接完成：

- 脚手架、依赖和配置；
- 重复性类型、样板 CRUD 和迁移框架；
- 测试环境、mock、fixture 和 CI；
- 机械性重命名、格式化和文档；
- 与本次概念无关的 UI 胶水；
- 用户已经多次证明掌握的重复模式。

骨架必须能编译或运行，不能交给用户一个同时存在十几个未知错误的半成品。

### 第三步：设置学习检查点

每个里程碑选择 1 至 2 个代表性任务让用户亲手实现。检查点应满足：

- 预计 20 至 60 分钟；
- 只考查一个主要概念；
- 已提供函数签名、类型、测试或明确输入输出；
- 有业务意义，不是人为制造的算法填空；
- 完成后能直接进入项目；
- AI 不提前给出完整答案。

推荐由用户实现的内容：

- 一个 TypeScript 领域类型与类型守卫；
- 一个 Vue 组合式函数或组件状态；
- 一个 ThoughtNode 自定义节点；
- 一个 Tiptap 稳定块 ID 扩展的核心逻辑；
- 一个 FastAPI service 或校验器；
- 一条 SQL/ORM 查询；
- 一个 ModelPatch 领域操作；
- 一个 Pydantic 业务约束；
- 一个核心 Vitest/Pytest 测试。

不适合要求用户手写的内容：

- 大段配置和安装命令；
- 第三方库样板；
- 数十个重复接口；
- 自动生成的 OpenAPI 客户端；
- 与学习目标无关的 CSS 搬运；
- 可由格式化器或代码生成器完成的机械劳动。

### 第四步：审阅而不是替换

用户提交检查点后，AI 应：

1. 先运行测试和类型检查；
2. 指出正确之处；
3. 按严重性说明错误和原因；
4. 优先给局部提示，让用户有一次修正机会；
5. 用户仍被阻塞或要求赶进度时，再直接修复；
6. 修复后解释从错误到正确的关键差异。

不要因为风格偏好重写用户的全部实现。保留能够正常工作的个人表达，除非它造成真实维护或正确性问题。

### 第五步：完成与复盘

AI 负责把功能收尾到可运行状态：

- 补齐边界情况；
- 完成测试；
- 运行 lint/typecheck/test/build；
- 检查错误、空、加载和取消状态；
- 更新学习台账；
- 只要 AI 实现或修改的代码中包含用户需要掌握的内容，无论该功能是否主要由 AI 完成，都必须把相关技术点按“必须掌握 / 理解即可 / 了解即可”标注到 `docs/mynotes/` 下对应里程碑笔记，并告知用户主要改动了哪些文件，方便回看与复习；
- 用 3 至 8 条要点总结本次业务与技术收获。

---

## 7. 三种协作模式

AI 根据任务选择并明确说明模式。

### 模式 A：AI 示范

适用于首次接触、脚手架或高风险配置。

AI 完成一个最小例子，随后要求用户解释关键数据流或完成一个小变体。示范代码必须小而完整，不一次生成整个业务模块。

### 模式 B：结对实现，默认模式

AI 完成接口、类型、测试框架和相邻样板，用户完成一个核心切片，AI 审阅并收尾。

推荐分工约为：

- AI 完成 60% 至 80% 的机械和支撑工作；
- 用户完成 20% 至 40% 的代表性核心代码。

比例不是绩效指标，应按任务风险调整。

### 模式 C：用户主导

适用于已经有基础、需要验证掌握程度的功能。

AI 只提供用户故事、验收测试、接口和分层提示。用户完成后，AI 进行代码审查和压力测试。

用户可以随时说“直接实现”“代做”或“赶进度”。此时 AI 应接管实现，不进行道德评价，但要在完成后安排更短的代码走读或相邻小练习。

---

## 8. AI 解释代码的要求

不要逐行翻译语法。解释优先回答：

1. 这段代码在业务链路中的位置；
2. 输入、输出和状态所有者是谁；
3. 为什么放在这一层；
4. 失败时会发生什么；
5. 为什么不用更简单或更复杂的方案；
6. 面试官可能追问什么。

新技术首次出现时，使用以下结构：

```text
为什么现在需要
最小心智模型
在本项目中的位置
本次只学习什么
暂时不学习什么
如何验证已经掌握
```

避免一次讲完整个框架。知识应在业务首次需要时出现，并在后续功能中重复使用。

---

## 9. AI 生成代码的边界

### AI Coding 也是待验证能力

每个里程碑至少保留一条可用于面试的 AI 协作记录：

- 任务如何拆分；
- 给 AI 的上下文、约束和验收标准；
- AI 生成或修改了哪些内容；
- 用户接受、修改或拒绝了什么；
- 发现了哪些幻觉 API、逻辑遗漏、安全或维护问题；
- 最终用哪些类型检查、测试、构建和运行结果验证。

AI 不得自行虚构“提效百分比”。只有存在可比时间或工作量记录时才量化。

### 必须做到

- 编辑前阅读现有实现和约定；
- 小步提交补丁，保持每一步可运行；
- 优先使用项目已有模式；
- 对新依赖说明业务理由和替代方案；
- 生成代码后亲自运行验证；
- 对 AI/并发/版本逻辑补测试；
- 主动检查依赖 API 是否真实存在、版本是否匹配；
- 把 AI 产出中的一个真实缺陷或人工修正记录到学习台账；
- 对不确定事实先查官方文档；
- 明确哪些代码来自模板、哪些是项目核心。

### 禁止

- 一次生成数千行后让用户自行理解；
- 只展示最终代码，不解释数据流；
- 为了使用热门技术改写业务；
- 未测量就进行复杂性能优化；
- 用 `any`、静默 catch 或禁用类型检查掩盖问题；
- 把模型输出直接写入已确认数据；
- 把框架对象直接当领域模型持久化；
- 没有测试就声称功能完成；
- 把安装成功记为用户已经掌握；
- 虚构用户、性能或准确率数据。

---

## 10. 技术准入检查

引入任何新框架、数据库或基础设施前，AI 必须简要回答：

1. 哪个已确认用户故事需要它？
2. 当前简单方案哪里不足？
3. 是否有现成依赖已经解决？
4. 新增多少学习、配置和维护成本？
5. 如何通过测试或指标证明收益？

没有明确答案时不引入，先记录到台账的“候选/未涉及技术”。

---

## 11. 完成定义

一个功能只有同时满足以下条件才算完成：

- 对应明确用户故事；
- 验收标准通过；
- 类型检查、相关测试和构建通过；
- 处理加载、空、错误和必要的取消状态；
- 没有泄露密钥或真实私人笔记；
- 关键取舍已写入文档或代码注释；
- 学习台账已更新；
- 用户至少能解释本次核心概念，或状态被诚实标记为“仅见过/待复习”。

“AI 已经写完”不等于“功能完成”，也不等于“用户已经学会”。

---

## 12. 学习台账维护规则

每完成一个可运行功能，更新 `Gyrnote-学习与实现台账.md`：

1. 功能状态和代码证据；
2. 使用的技术；
3. 用户参与方式；
4. 掌握等级；
5. 测试或验证证据；
6. 仍不清楚的问题；
7. 下一次复用机会；
8. AI 协作、人工审查和验证证据。

掌握等级只能使用：

| 等级 | 含义 |
|---|---|
| 未涉及 | 项目还没使用 |
| 已见过 | AI 实现并讲解，用户尚未动手 |
| 引导实现 | 用户在接口/提示下完成 |
| 能独立解释 | 能说明数据流、边界和取舍 |
| 能独立修改 | 能完成相邻需求或排查常见错误 |
| 已巩固 | 在不同功能中再次使用并通过验证 |

不能仅凭用户说“看懂了”直接标记“已巩固”。需要代码、解释、测试、修复或相邻任务作为证据。

---

## 13. 安全与隐私

- `github_api_token.txt` 已在 `.gitignore` 中，任何 AI 不得读取并输出其内容；
- 模型 API Key 只存在后端环境变量；
- 日志不得记录完整私人笔记、token 或认证信息；
- 示例数据使用公开、虚构或经用户允许的内容；
- 富文本、Markdown 和 AI 返回内容必须考虑 XSS；
- 不执行破坏性 Git 或文件操作；
- 未经用户明确要求，不提交、推送或发布代码。

---

## 14. 当前起点

当前尚未开始 Gyrnote 代码实现。第一个里程碑是 M0 工程底座：

1. 建立 `apps/web` 与 `apps/api`；
2. 前后端能够启动；
3. PostgreSQL 和 Redis 可通过 Docker Compose 启动；
4. 建立最小健康检查；
5. 建立 lint、typecheck、test、build；
6. 生成一次前端 API client；
7. 更新学习台账。

M0 的主要学习检查点应集中在：

- 用户解释前后端请求链路；
- 用户亲手完成或修改一个类型化健康检查调用；
- 用户理解环境变量为什么分前端公开变量和后端秘密变量。

不要在 M0 接入 Tiptap、Vue Flow、LLM、向量数据库或 Agent。
